image: alpine

pages:
  script:
  - apk --no-cache add py3-pip python-dev
  - pip install sphinx
  - apk --no-cache add make
  - cd doc
  - make html
  - mv build/html/ public/
  artifacts:
    paths:
    - public
  only:
  - gitlab-pages-test
