image: fenicsproject/stable

before_script:
  - ps -p$$ -ocmd=

pages:
  script:
  - cd doc
  - make html
  - cd ../
  - mv doc/build/html/ public/
  artifacts:
    paths:
    - public
  only:
  - master
